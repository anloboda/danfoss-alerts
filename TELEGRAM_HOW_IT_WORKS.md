# Як працює надсилання повідомлень через Telegram Bot

## Загальна схема:

```
[AWS Lambda] → [Telegram Bot API] → [Telegram Server] → [Ваш Telegram]
```

## Компоненти:

### 1. **Bot Token** (your-bot-token)

Це **"пароль"** вашого бота. Він потрібен щоб:
- **Ідентифікувати бота** - Telegram знає, що це саме ваш бот
- **Автентифікувати запити** - Telegram перевіряє, що ви маєте право керувати цим ботом
- **Надсилати API запити** - кожен запит містить токен

**Аналогія**: Це як логін/пароль для вашого бота

### 2. **Chat ID** (331848921)

Це **"адреса"**, куди надсилати повідомлення. Telegram потребує знати:
- **Кому саме** надсилати повідомлення
- В **який чат** (особистий чат з ботом, група, канал)

**Аналогія**: Це як номер телефону або email адреса - потрібен щоб знати куди надсилати

## Детальне пояснення Chat ID:

### Що таке Chat?

Chat - це будь-яка розмова в Telegram:
- **Особистий чат** - розмова між вами та ботом (один на один)
- **Груповий чат** - коли бот доданий в групу
- **Канал** - публічний канал

### Чому потрібен Chat ID?

Telegram працює так:
1. Кожна розмова має **унікальний ID**
2. Щоб надіслати повідомлення, потрібно вказати **цей ID**
3. Бот **не може** самостійно знайти ваш чат - ви маєте сказати йому ID

### Як працює надсилання:

```javascript
// Lambda функція робить HTTP запит:
fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
  method: "POST",
  body: JSON.stringify({
    chat_id: "331848921",  // ← ТУТ вказуємо Chat ID
    text: "Ваше повідомлення"
  })
})
```

**Що відбувається:**
1. Lambda відправляє запит на `https://api.telegram.org/bot<TOKEN>/sendMessage`
2. В запиті вказується `chat_id: 331848921` (ваш особистий чат)
3. Telegram отримує запит, перевіряє токен
4. Telegram знаходить чат з ID `331848921`
5. Telegram надсилає повідомлення в цей чат
6. Ви отримуєте повідомлення в Telegram!

## Візуальна схема:

```
┌─────────────────┐
│   AWS Lambda    │
│ (Temperature    │
│  Check Func)    │
└────────┬────────┘
         │
         │ HTTP POST
         │ https://api.telegram.org/bot<TOKEN>/sendMessage
         │ Body: { chat_id: "331848921", text: "..." }
         │
         ▼
┌─────────────────┐
│ Telegram Bot    │
│      API        │
│                 │
│ Перевіряє:      │
│ ✓ Bot Token     │
│ ✓ Chat ID       │
└────────┬────────┘
         │
         │ Знаходить чат з ID 331848921
         │
         ▼
┌─────────────────┐
│ Telegram Server │
└────────┬────────┘
         │
         │ Надсилає повідомлення
         │
         ▼
┌─────────────────┐
│ Ваш Telegram    │
│ (Chat ID:       │
│  331848921)     │
│                 │
│ 💬 Отримуєте    │
│ повідомлення!   │
└─────────────────┘
```

## Чому саме Chat ID, а не username (@superloboda)?

**Можна використовувати і username**, але є переваги Chat ID:

### Chat ID (331848921):
✅ **Швидше** - не потрібно шукати за username  
✅ **Надійніше** - ID не змінюється, username може змінитись  
✅ **Працює завжди** - навіть якщо ви зміните username  
✅ **Приватніше** - не видно ваше username в коді  

### Username (@superloboda):
⚠️ Може змінитись  
⚠️ Трохи повільніше (потрібно спочатку знайти за username)  
✅ Простіше запам'ятати  

**В нашому коді використовуємо Chat ID (331848921)** для надійності.

## Як отримати Chat ID?

### Варіант 1: Через @userinfobot
1. Відкрийте Telegram
2. Знайдіть бота `@userinfobot`
3. Надішліть `/start`
4. Бот покаже ваш ID: `331848921`

### Варіант 2: Через Bot API
1. Почати чат з вашим ботом (написати `/start`)
2. Відкрити в браузері:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
3. Знайти в відповіді `"chat":{"id":331848921}`

## Чому бот не може просто надіслати вам повідомлення?

**Telegram працює за принципом "opt-in"** (потрібна згода):

1. Ви повинні **спочатку почати чат** з ботом (написати `/start`)
2. Тільки **після цього** бот може надсилати вам повідомлення
3. Бот **не може** самостійно знайти ваш аккаунт та надіслати повідомлення

**Аналогія**: Це як підписка - ви спочатку підписуєтесь, а потім отримуєте повідомлення.

## В нашому коді:

```typescript
// Lambda функція використовує:
const url = `https://api.telegram.org/bot${botToken}/sendMessage`;

await fetch(url, {
  method: "POST",
  body: JSON.stringify({
    chat_id: chatId,        // ← Ваш Chat ID: 331848921
    text: message,          // ← Текст повідомлення
    parse_mode: "Markdown"  // ← Форматування (жирний, курсив)
  })
});
```

**Результат**: Telegram знає куди (Chat ID) і що (текст) надіслати!

---

## Підсумок:

- **Bot Token** = "Хто" (який бот надсилає)
- **Chat ID** = "Кому" (куди надсилати)
- **Text** = "Що" (що надіслати)

Без Chat ID Telegram не знає куди надіслати повідомлення! 🎯

